# Порт в Godot — что осталось добавить

Сверка полного дизайна браузерного оригинала (`../Potion_game_git`: CLAUDE.md, ROADMAP.md)
с текущим кодом порта. ROADMAP.md в оригинале отражает прогресс БРАУЗЕРНОЙ версии — здесь
трекинг именно ПОРТА.

## ✅ Уже есть в порте
- Кор-луп: ЗАПОМНИ → ВОССОЗДАЙ → результат; 4 параметра + **накал (sat)** (УР.4).
- Аркадный цикл: дни/стадии/пул, прогрессия (xp/уровни), STAGE_TABLE, тиры.
- Чаевые (экономика заработка) — есть.
- Репутация с NPC; грейды (идеал/годно/**пойло**/брак) + базовые стикеры.
- Ачивки (общие + NPC) — данные и экран (mojibake починен).
- Профиль (сохранение), аккаунты (Supabase, `potion_auth.gd`).
- Флейворы/реплики персонажей.
- Механики NPC: tentacloid, trucker_chrome (+gear-path), fashionista, archivist (+matrix rain).
- Каркас механик: `setup/memorize_start/craft_start/process/stop/weight_for/result_note`, `_key_score`.

## 🔴 Крупные системы — НЕ реализованы в порте
1. **Звук / SFX** — 🚧 КАРКАС ГОТОВ (`scripts/sfx.gd`, autoload `Sfx`, шины Music/SFX,
   реальные mp3 из `assets/sound/` + музыка `assets/track_*` с кросс-фейдом меню/игра, duck).
   Ассеты скопированы из браузерного репо. Базовые события подключены: tick (ползунки),
   brew (серв), perfect/good/bad (грейд), achieve (левелап), cardPick, orderShow, weekEnd,
   uiClick/dock/badPop в механиках. **UI громкости** — есть (2 слайдера в меню, сохраняются
   в профиль `settings.music_vol/sfx_vol`). **Осталось:**
   доп. события — liquidUp/Down (объём), colorShift (спектр), stir,
   countdown (последние 3с фазы), blobDrag (vex). (blobGrab/Snap — у swarm; meow/pawClick — у catlady;
   бит Диджея — синтезируется в dj_pulsar; countdown — у racer_kai.)
   Примечание: `good`/`badClear` без своих файлов → фолбэк (perfect/badPop). mp3 импортируются
   Godot при первом открытии редактора.
2. **Магазин и инвентарь** (Фаза 6) — предметы, грейды предметов, эффекты на заказ
   (напр. «джиггер» отключает регулятор), трата чаевых. В профиле поля заведены, логики нет.
3. **Умения игрока** (Фаза 7) — skill_1..4 (активные/пассивные способности игрока).
4. **Модификаторы / фокус-заказы** — фокусные параметры (вес ×2.2 / прочее ×0.55), иконки
   фокуса, фразы. Влияет на скоринг (в `_do_finish` фокуса пока НЕТ). modifiers/new3/multi.
5. **Квесты** — заказы-цели + закрепление (quests_pin).
6. **Взаимоотношения NPC** — граф friend/buddy/enemy/dislike (`NPC_RELATIONS`), реакции на
   карточке, разовый эффект на репутацию, эскалация «обиды» (грудж 3 → форс-брак, 6 → уход
   из пула). Пороги по тирам 1/2-3/4-5.
7. **Ежедневный особый заказ** — сид дня (UTC), 30 слотов (10×3), профили сложности,
   отдельный лидерборд. Сейчас кнопка-заглушка «скоро» (disabled).
8. **Глобальный лидерборд игроков** — соревнование «кто больше рейтинга за цикл». В коде есть
   только НЕИСПОЛЬЗУЕМЫЕ backend-функции `leaderboard_load/save` (`potion_auth.gd`). Нужно:
   отдельная иконка 🏆 в топбаре → панель «Глобальный рейтинг» (имя · счёт · дата, `leaderboard_load`);
   отправка `cycle_score` в лидерборд по концу цикла (`end_cycle` → `leaderboard_save("arcade", ник, cycle_score)`).
   Постоянное соревнование = ядро ретеншена, приоритет высокий.
9. **Кооператив** (Фаза 12) — нужен realtime-бэкенд. Низкий приоритет.
10. **Реворк пассивок персонажей** (Фаза 11).

## 🟡 Особые типы заказов + недостающие параметры банки
Особые `type`/`special` (меняют набор регуляторов на ВСЕХ уровнях) — данные в NPC есть,
игровой логики нет:
- **shape** (nebula_chef) — угадывание ФОРМЫ сосуда.
- **gradient** (twofaced_priestess) — два независимых спектра.
- **moving** (plasma_bartender) — физически летающие сгустки + регулятор скорости.
- **dual_size** (supernova_child) — ширина и высота раздельно (+поворот на УР.4).
- **no_timer** (the_waiter) — без таймеров, банка-часы, рейтинг только при >99%.
- **trust** (last_of_ir) — меню доверия / бафф-дебафф следующему заказу.

Для них (и для части механик) нужны НОВЫЕ параметры рендера банки:
`colorB` (2-й цвет/градиент), `shape` (SHAPE_PROFILES — формы сосуда), `size2` (высота
отдельно), `countB` (2-й счётчик), `rotation` (поворот), `speed` (скорость полёта).

## 🎭 Механики NPC — осталось (полный разбор в NPC_MECHANICS.md)
Готово 19/23 — ВСЕ «обычные» механики (tentacloid, trucker_chrome, fashionista, archivist,
catlady, logic9-база, racer_kai, apothecary_mo, janitor, marketer, dj_pulsar, drone, perfumer,
gourmet_vega-база, swarm_navigator, guild_inspector-база, engineer-база, collector_gz, vex).
Готово 24: + plasma_bartender (physics полёта сгустков + параметр `speed`). Осталось всего
**1 механика** — nebula_chef (`shape`: угадывание формы банки; нужны несколько силуэтов бутыли,
сейчас один PNG → требует арта/процедурного контура). Довески по готовым: градиент Двуликой,
форма-часы Тот-Кто-Ждёт, меню доверия Ир (УР.1/2), бонус-мини-игры УР4, поворот Сверхновой.
(intern_beep, pete — механики нет.) Каркасные хуки:
- **`score_bonus()`** — ГОТОВО. repBonus — TODO.
- **`skip_memorize()`** — ГОТОВО (Инспектор/Инженер).
- **`override_overall()`** — ГОТОВО (Коллекционер — верно/неверно).
- **ось-скоринга «позиция»** — для vex (расстановка сгустков). НЕ сделан.
- **score-override по зоне** — для engineer-УР4 (красная ловушка). НЕ сделан.

## 🧩 Сквозные / инфраструктура
- **Арт-подмена** — эмодзи-плейсхолдеры → картинки (лапы кошатницы, детали Роя, панели
  магазина/инвентаря, иконки умений, кастомные банки). Единый хелпер загрузки.
- **Локализация EN** — тексты пока только RU (в `game_data.gd` помечено «EN добавим позже»),
  включая фокус-фразы (ff).
- **Стикеры-спецы** — условные особые стикеры (STICKER_SPECIALS) + платиновый.
- **Полировка** (Фаза 13) — финальная оптимизация/чистка.

## Зависимости / рекомендуемый порядок
1. **Звук** — дёшево, сразу поднимает game feel, нужен всем механикам.
2. **Механики без новых параметров** (catlady, logic9, engineer, marketer, gourmet, dj_pulsar) +
   каркасный `score_bonus` (открывает racer/apoth/janitor).
3. **Новые параметры банки** (gradient/shape/size2/countB/speed/rotation) — разблокируют
   особые типы + оставшиеся механики (perfumer/collector/plasma/twofaced/supernova/nebula/waiter/vex).
4. **Системы прогрессии-контента**: модификаторы/фокус → магазин → умения → квесты → связи NPC.
5. **Ежедневный режим** (использует лидерборд, уже есть каркас).
6. **Кооп** — в самом конце (realtime backend).
