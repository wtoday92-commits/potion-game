# Порт в Godot — что осталось добавить

Сверка полного дизайна браузерного оригинала (`../Potion_game_git`: content.js, game.js,
CLAUDE.md, ROADMAP.md) с ФАКТИЧЕСКИМ кодом порта. Обновлено: 2026-08-17.

## ✅ Уже есть в порте
- Кор-луп: ЗАПОМНИ → ВОССОЗДАЙ → результат; параметры цвет/накал/объём/сгустки/размер (+ накал на УР.4).
- Аркадный цикл: дни/стадии/пул, прогрессия (xp/уровни), STAGE_TABLE, тиры, стадийная машина.
- Выбор заказа: карточка дня (1 из 3) + выбор сложности **УР.1–4** (УР.4 гейтится репутацией с гостем).
- Чаевые (заработок копится), репутация с NPC, грейды (идеал/годно/пойло/брак) + базовые стикеры.
- Ачивки (общие + NPC), профиль (сохранение), аккаунты (Supabase, `potion_auth.gd`).
- Звук/SFX: каркас (`Sfx`, шины, музыка с кросс-фейдом) + многие события подключены.
- Флейворы/реплики; портреты; параллакс-камера; тема бара (стол/окно/лампы).
- **Механики NPC: 24 из 25** — все `LEVEL4_FX` + Пит (см. ниже). Не хватает 1 — nebula_chef.
- **Особые типы**: trust (Ир — баффы/дебаффы/чип/replay/force_replay), dual_size (Сверхнова +
  наклон УР.4), no_timer (Тот-Кто-Ждёт), moving (Бармен — физика полёта + `speed`),
  **gradient (Двуликая — два спектра/вертикальный градиент на всех УР + два счётчика на УР.4,
  с разведением спектров по кругу)**.
- Навигатор Роя — переделан в **игру на память** (запомнить набор деталей → собрать их из десятков).
- Сгустки: лимит **18** (Двуликая 7+7), размер привязан к объёму банки + адаптивная упаковка.
- **Модификаторы заказов + фокус** (фокус: вес ×2.2/×0.55 + награда; timer/duck/rampage) — на карточках дня, тултипы, гейтинг прогрессией.
- **«Грейд выше своего»** — низкий тир изредка выпадает как высокий (числа целевого тира + награда), пометка на карточке.
- **Глобальный лидерборд** — панель 🏆 (онлайн Supabase + локальный fallback), авто-отправка `cycle_score` в конце цикла.
- **Магазин + предметы** (Фаза 6) — 10 расходников ×3 грейда, инвентарь, покупка за чаевые, панели 🛒/🎒, гейтинг грейдов по прогрессии. Эффекты: время/память/соль/шейкер/фишка/трос/глаз/джиггер. TODO: repboost (Паприка), speedlock (Ускоритель).
- Инспектор — художественное «Дело о приёмке» + графическая папка; Аптекарь — размытие/десат банки + ЭКГ; Бабушка — лапки-картинки.

## 🔴 Крупные системы — НЕ реализованы в порте
1. ✅ **Умения игрока** (Фаза 7) — ГОТОВО 2026-08-23. 4 умения на экране дня, тратят заряды
   (потолок 3, +1 за 3 идеала): 👀 «Кто там?» (гарант. гость), 🚫 «Не пускать» (бан до 3),
   🔄 «Обновить гостей дня», 🔁 «Грейд выше». Гейтятся прогрессией skill_1..4. Заряды в профиле.
3. ❌ **Квесты** — УДАЛЕНЫ из игры по решению (2026-08-23); флаги quests/quests_pin вырезаны.
4. ✅ **Связи NPC** — ГОТОВО 2026-08-23. Граф `NPC_RELATIONS`, откр. с xp14000 + порог реп.
   При выборе гостя дня: друг/собутыльник +реп; враг/неприязнь копят грудж и −реп (грудж 3 →
   «обида»: форс нечестного стикера bad9; 6 → уход из пула на цикл). Состояние за цикл в профиле.
5. ✅ **Ежедневный особый заказ** — ГОТОВО 2026-08-23 (MVP). Детерминированная тройка по дате UTC,
   3 сложности (tier 3/4/5), 10 дней, отдельный топ ("daily"), прогресс изолирован (бэкап/откат
   профиля). Гейты: умения/связи в дейлике выключены.
6. **Кооператив** (Фаза 12) — realtime-бэкенд. Низкий приоритет.
7. ✅ **Полоса поощрений цикла** — ГОТОВО. Второй топбар: «Лавка ур.N» + полоса с
   отметкой на каждый день цикла. Годно → золотой стакан, идеал → крупнее, с
   «дыханием» и свечением, 100% → именной стакан гостя (27 шт.), пойло → каменная
   заглушка, брак → разбитая тарелка. Градация 0..4 (годно+ поднимает, брак
   опускает, пойло не трогает) красит полосу от серой до золотой; 5 идеалов подряд
   → секретная фиолетовая. В конце цикла градация умножает рейтинг
   (×0.75 / 0.85 / 1.0 / 1.2 / 1.5 / 1.75) и с 4-й градации доливает репутацию
   гостям с идеальными отметками. Шейдер `shaders/reward_track.gdshader`,
   виджет `scripts/reward_track.gd`, звуки `track*.mp3`.
   День/серия/рейтинг/стикеры переехали в **выездной топбар**: треугольник-язычок
   над панелями, тап/свайп вниз — выезжает поверх обоих, уходит сам через 4 с
   или свайпом вверх.

## 🟡 Особые типы + недостающие параметры банки
- 🔴 **shape** (nebula_chef) — угадывание ФОРМЫ сосуда. Нужны несколько силуэтов бутыли
  (сейчас один PNG → арт/процедурный контур). Единственная непортированная механика персонажа.
- Недостающие параметры рендера банки: `shape` (формы сосуда).
  Готовы: `colorB` (2-й спектр/градиент), `size2`, `rotation`, `speed`, `countB`, `fill`.

## 🎭 Довески к готовым механикам
- Меню доверия Ир на УР.1/2 (баффы/дебаффы работают, «диалога доверия» нет).
- Возможные бонус-мини-игры УР.4 у отдельных персонажей.
- (intern_beep, pete — спец-механики нет и в оригинале; Пит — только «уровень жидкости», есть.)

## 🎨 Прибирание визуала (по аудиту 2026-08-25)
- ✅ **Этап 1 — дизайн-система.** `scripts/ui.gd` (`class_name UI`): типошкала из 7 ступеней,
  палитра ролей, шаг сетки кратно 4, подложки (панель/карточка/ячейка), кнопки
  (главная/обычная/тихая) и общая `Theme`. Кегли и отступы числами вычищены полностью
  (было 155 и 105 → 0), 24 разных кегля свелись к 6.
- ✅ **Этап 2 — интерфейс отделён от арта.** Между параллаксом и UI появился слой-скрим
  (`_scrim()`), глубина зависит от экрана: раунд 0.0 (бар и есть сцена), день/выбор/результат
  0.42, меню и служебные экраны 0.66. Карточки внутри панелей сделаны непрозрачными,
  тень у подписей смягчена (была костылём читаемости поверх арта). Замер яркости
  одного и того же участка фона: 46 → 17 → 13.
- ✅ **Этап 3 — пересобраны четыре главных экрана.** Меню: заголовок на плашке, три
  счётчика схлопнуты в одну статус-строку, плитки тянутся на всю высоту, громкость
  уехала в настройки (там те же ползунки уже были). День: «без модификатора» не
  рисуется вовсе, «← В меню» стала нормальной кнопкой внизу вместо 120px в углу.
  Раунд: подписи значений с FS_M до FS_L, на фазе показа пустую полосу занимает
  подсказка «Запомни: …». Результат: блоки выровнены по одной ширине, разбивка —
  полоски-шкалы вместо мелких процентов.
  ⚠️ Экран выбора сложности (четыре одинаковых серых бруска) из аудита — не трогал.
  **Проверять раскладку на ПОЗДНЕЙ прогрессии, а не на текущем профиле:** при пуле в
  4 гостя кнопку «В меню» выдавливало за экран (низ занят сумкой, панелью умений и DEV).
  Прокрутку убрали: размер карточки считается от их количества (`_card_av_for`),
  DEV-кнопка уехала в настройки, «В меню» встала в освободившийся правый угол
  вровень с сумкой и панелью умений. Проверено на 2 и 4 карточках,
  на раунде и результате с максимумом параметров (Двуликая, УР.4 — 6 строк разбивки).
- ⏳ Этап 4 — пустые состояния (ачивки «???», ряды замков, пустая полоса наград).
- ⏳ Этап 5 — отделка (тосты поверх экранов, DEV-кнопка, единый гейтинг, цвета тиров).

## 🧩 Сквозные / инфраструктура
- **Арт-подмена** — часть элементов ещё процедурные/эмодзи-плейсхолдеры (панели магазина/умений,
  иконки способностей, кастомные формы банок). Детали Роя/лапы кошатницы — нарисованы кодом.
- **Локализация EN** — тексты пока только RU.
- **Спец-стикеры** (STICKER_SPECIALS условные + платиновый) — частично.
- ✅ **Пассивки персонажей** — ГОТОВО 2026-08-24. 27 гостей × 5 пассивок (данные из
  `NPC_PASSIVES`, реворк Фазы 11 уже учтён): открываются уровнями репутации, до 3
  активных за цикл, состав замораживается после первого выполненного заказа.
  Панель ⚡ в топбаре + выбор прямо на странице гостя. Эффекты: score / craftTime /
  memTime / speedCap / rep / progress / tips + уникалки chargeAt2, tipsFlat.
  В дейлике выключены (как умения/связи). UI: квадратные кнопки-иконки в ряд,
  первый тап — контекстное меню (название/эффект/область), повторный — вкл./выкл.
  Арт: 135 иконок пассивок (`assets/ui/passive_<npc>_<pid>.png`) + 27 иконок гостей
  (`npcicon_<id>.png`) — сгенерированы под стиль игры, эмодзи остались фолбэком.
- **Звук** — доп. события: liquidUp/Down (объём), colorShift (спектр), stir, countdown (последние 3с).

## Рекомендуемый порядок (по влиянию на игру)
1. **Фокус-заказы + модификаторы** — дают смысл вариативности заказов, дёшево, влияют на скоринг.
2. **«Грейд выше своего»** — оживляет пул (низкие тиры иногда «страшнее»), масштабирует награду.
3. **Магазин + предметы** — куда тратить чаевые (петля заработка).
4. **Глобальный лидерборд** — ядро ретеншена (каркас backend уже есть).
5. **Умения игрока → квесты → связи NPC**.
6. **Ежедневный режим** (использует лидерборд).
7. **Контент-довески**: nebula_chef (форма), градиент Двуликой.
8. **Кооп** — в самом конце (realtime backend).
