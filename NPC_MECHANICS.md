# Каталог механик NPC (инспекция браузерного оригинала)

Источник: `C:\Users\KDFX Modes\Documents\Potion_game_git\game.js` — диспетчер `LEVEL4_FX[id]`
(+ несколько механик живут вне диспетчера). Здесь — ПОЛНЫЙ разбор для порта в Godot.

## Как устроен диспетчер (оригинал)

`LEVEL4_FX[id]` — объект с необязательными хуками:
- `setup()` — сразу после `computeActiveKeys`, до первого показа банки; рандомит свои поля `target`.
- `memorizeStart()` — доп. эффект поверх обычной фазы «ЗАПОМНИ».
- `replaceMemorize(onDone)` — полностью берёт тайминг фазы показа на себя (взаимоискл. с `memorizeStart`).
- `craftStart()` — сразу после настройки фазы «ВОССОЗДАЙ».
- `stop()` — уборка (интервалы/DOM/слушатели), зовётся и в начале нового заказа, и в конце.
- `scoreBonus(scoreData, timeFrac)` → `{ ratingMultAdd, repBonus, thresholdOverride }` — в `finalizeResult`, до расчёта delta.

Гейт активности: `mechActive(id) = (regLevel === 4 || id ∈ MECH_FROM_L1)`.
`MECH_FROM_L1` (~18 гостей — механика с УР.1): drone, janitor, trucker_chrome, collector_gz,
fashionista, tentacloid, dj_pulsar, logic9, racer_kai, apothecary_mo, perfumer, swarm_navigator,
vex, guild_inspector, gourmet_vega, catlady, engineer, marketer.

Особые `cfg.special` (меняют НАБОР ползунков на всех уровнях, см. `computeActiveKeys`):
shape (nebula_chef), gradient (twofaced_priestess), moving (plasma_bartender),
dual_size (supernova_child), no_timer (the_waiter), trust (last_of_ir).

## Godot: текущий каркас и разрыв с оригиналом

`scripts/npc_mech.gd` — `NpcMech` (RefCounted) + подклассы, диспетчер `NpcMech.make(id)`.
Хуки уже есть: `setup / memorize_start / craft_start / process(g,delta) / stop / weight_for(key,base) / result_note`.
Вшито в `main.gd`: гейт `GameData.mech_active`, вызовы хуков (в т.ч. `process` в фазе «ВОССОЗДАЙ»),
`weight_for` в `_do_finish`, `result_note`, помощник `_key_score(key)` (насколько верно выставлен ползунок).

**Не хватает для полного порта (надо добавить в каркас):**
- `score_bonus()` → множитель рейтинга — ГОТОВО (используют racer_kai, apothecary_mo; далее janitor, logic9-УР4). repBonus пока не портирован (только множитель очков).
- `replace_memorize()` — своя фаза показа (guild_inspector, collector_gz — показа нет вообще).
- per-frame `process(g,delta)` — ГОТОВО (используют archivist, catlady, racer_kai, apothecary_mo).
- Доступ механики к банке/оверлеям (уже есть через `g`: jar, jar_stage, round_ui).

**Модель параметров в Godot упрощена — 4 ползунка:** `color, volume(=size), count, bsize`.
В Godot ДОБАВЛЕН `sat` («Накал», насыщенность) — активен только на УР.4, колонка сразу после
цвета. В оригинале ещё есть (в Godot пока НЕТ): `colorB` (градиент), `size2` (высота),
`countB` (2-й счётчик), `shape` (форма банки), `rotation` (поворот), `speed` (скорость),
`vexPosition` (позиция сгустков).
Механики, завязанные на отсутствующие параметры, требуют либо адаптации на 4-модель, либо
добавления параметра.

Легенда сопоставления: ✅ ложится на 4-модель · 🟡 нужен новый параметр/ось или доп. хук · 🔴 требует параметра, которого нет (адаптировать/добавить).

---

## Механики с УР.1 (MECH_FROM_L1)

### tentacloid — Тентаклоид ✅ ГОТОВО
Считается только ОДИН случайный активный параметр (вес остальных = 0); какой — раскрывается
на результате. На УР.4 — ДВА скрытых, берётся ХУДШИЙ из их score.
Хуки: `setup` (выбор параметра), скоринг (`weight`), `result_note`.
Порт: реализован (`TentacloidMech`, вес через `weight_for`).

### trucker_chrome — Хромой Дальнобойщик ✅ ГОТОВО
Цвет (левый ключ, `TRUCKER_LEFT_KEYS`) — обычный ползунок. Правая группа (объём/сгустки/размер)
заменяется на ОСОБЫЙ регулятор `makeGearPathWidget`: ломаная траектория (1 из 10 форм
`TRUCKER_GEAR_PATHS`), рычаг тянется ПО ПУТИ, значение = доля пути. Видно по одному правому
регулятору за раз, стрелка переключает «передачу» по кругу. +время на варку. УР.4 добавляет
инерцию рычагам (не портируем пока).
Хуки: `craftStart` (замена виджетов + кнопка), `stop`.
Порт: `TruckerMech` + `scripts/gear_path.gd` (регулятор-рычаг). Инерция УР.4 — TODO.

### fashionista — Модница ✅ ГОТОВО
На «ВОССОЗДАЙ» доступен только ОДИН ползунок; кнопка «готово с этим» идёт по перемешанному
кругу (каждый посещается 1 раз за круг). УР.4: «Дальше» не пустит, пока текущий не идеален
(`l4FashionKeyPerfect`), + возмущённая реплика и +1.5с к показу.
Порт: `FashionMech` — lock всех кроме одного (`editable=false`+затемнение), стрелка-кнопка
справа идёт по кругу; УР.4-гейт через `g._key_score(key) >= 0.98` + тост-реплика.

### logic9 — Логика-9 ✅ ГОТОВО (база) / 🟡 (бонус УР.4)
База: ползунки заменены на СТЕППЕР (`▲`/`▼` по делению), +50% времени на варку.
УР.4-бонус: после ≥годноты — мини-игра «самолётик сбивает сгустки», доля сбитых → множитель
рейтинга +0..+50%.
Порт базы: `LogicMech` — прячет слайдер, ставит VBox из ▲/▼ (шаг по `s.step`, пишет в слайдер,
тик), ×1.5 время. Мини-игра УР.4 — TODO (нужен `score_bonus` + отдельная фаза-оверлей).

### racer_kai — Гонщица Кай ✅ ГОТОВО
3 чекпоинта по ходу таймера (0.33/0.66/0.9). На каждом замеряется overall; ≥0.55 → +5% к
множителю рейтинга. Отсчёт «3…2…GO!» + лёгкая тряска банки.
Порт: `RacerMech` — через `process` (elapsed vs доли времени), `_current_overall()`, `score_bonus`
= 1+0.05·пройдено; всплывающий Label-отсчёт + шейк `g.jar` + `countdown`.

### apothecary_mo — Аптекарь Мо ✅ ГОТОВО
Вертикальная полоска «состояние пациента» тает за время варки (зелёная→красная); чем раньше
финиш — тем больше остаток → бонус. `scoreBonus` = 1 + 0.5·остаток.
Порт: `ApothecaryMech` — полоска-Panel+ColorRect у левого края, обновление через `process`,
`score_bonus` от `g.phase_left/phase_total`. TODO: ЭКГ и фильтр банки (УР.4), repBonus.

### janitor — Уборщик Пятого Дока ✅ ГОТОВО
Банка залита грязью (обе фазы); палец-«губка» оттирает. Доля отмытого → бонус к рейтингу.
Размер губки меньше с уровнем.
Порт: `JanitorMech` + `scripts/grime_overlay.gd` (сетка покрытия 8×13, стирание пальцем,
`clean_fraction`); `score_bonus` = 1+0.5·clean. TODO: возврат грязи и ЭКГ-нет (УР.4), repBonus.

### drone — Штатный поставщик (Дрон) ✅ ГОТОВО
На «ВОССОЗДАЙ» внутри банки растут «плохие пузыри»; тапнуть до взрыва. Лопнул сам → сбивает
случайный активный регулятор на деление. УР.4: лопать только «курсором с инерцией» — TODO.
Порт: `DroneMech` + `scripts/bad_bubble.gd` (растущий пузырь, тап=cleared / дорос=burst);
слой поверх `jar_stage`, спавн через `process`, `_jitter()` сбивает слайдер при взрыве
(`badClear`/`badPop`). Кол-во/скорость роста/интервал — по уровню.

### catlady — Бабушка Мурра (кошачьи лапы) ✅ ГОТОВО
На «ВОССОЗДАЙ» на ползунки вылезают лапы и НЕ уходят сами; согнать — серией из 3 быстрых тапов
(иначе только трясётся). Согнанная возвращается через паузу; больше лап и короче пауза с уровнем.
Скоринг не трогает.
Порт: `CatladyMech` + `scripts/cat_paw.gd` (лапа поверх TouchSlider, ловит серию тапов, meow/pawClick),
респавн через `process`-хук. MAX/RETURN по уровню.

### engineer — Инженер навигатора (бегающий указатель) ✅ ГОТОВО (база)
Фазы показа НЕТ — цель показана ЗОНАМИ на треке. Указатель бегает синусоидой, «СТОП» фиксирует
значение = позиция указателя. В базе оценка обычная (по близости) — зоны как визуальная цель.
Порт: `EngineerMech` (через `skip_memorize`) + `scripts/eng_track.gd` (осциллятор-указатель,
зелёная зона + ядро, «СТОП» пишет в ползунок). TODO: красная зона-ловушка + score-override (УР.4).

### marketer — Маркетолог (хаос-панель) ✅ ГОТОВО
На «ВОССОЗДАЙ» обычные колонки прячутся; панель из ~10 случайных контролов, где для каждого
параметра работает РОВНО ОДИН, остальные — обманки. +50% времени.
Порт: `MarketerMech` — прячет слайдеры, строит GridContainer из stepper/slider (реальные пишут
в слайдер, декои — no-op+uiClick), ×1.5 время. Банка — единственный фидбэк. TODO: тип «крутилка»,
фон-газета, плывущий текст (УР.4).

### perfumer — Парфюмер (2D-пэд) ✅ ГОТОВО
Цвет×накал — один 2D-пэд вместо двух ползунков (X=накал, Y=спектр). УР.4: пэд → цветовой КРУГ.
Порт: `PerfumerMech` + `scripts/color_pad.gd` (сетка цветов с полом накала 30%, курсор, пишет
в оба ползунка). Накал добавляется в `active` в `setup` (у него активен на всех уровнях).
TODO: вариант-круг на УР.4.

### collector_gz — Коллекционер (сетка баночек) ✅ ГОТОВО
Показ обычный (образец запоминаем). На игре — сетка готовых зелий (2×2…4×4 по уровню), выбрать
совпадающее (по спектру и числу сгустков). Верно → идеал, иначе → брак. Декои — near-miss.
Порт: `CollectorMech` через новый хук `override_overall(g)->float`; сетка мини-банок
(переиспользует `PotionJar`), тап финиширует. Форму (`shape`) в Godot нет — сравнение по цвет+кол-во.

### swarm_navigator — Хозяин Роя (детали-магниты) ✅ ГОТОВО
На «ВОССОЗДАЙ» детали (⚙🔩…) на кольце вокруг банки; тащишь в зону банки, `count` = число внутри.
Деталей ровно target.count (все внутри = верно). +время. УР.4: дрейф — TODO.
Порт: `SwarmMech` + `scripts/drag_part.gd` (перетаскиваемая иконка); слой над `jar_stage`,
зона-хит-тест, счёт пишется в ползунок count (скрыт). Звуки blobGrab/blobSnap.

### guild_inspector — Инспектор Гильдии (лист «Допуски») ✅ ГОТОВО (база) / 🟡 (УР.4)
Фазы показа НЕТ — вместо банки лист «Допуски»: цель как «№X из N» по делениям каждого ползунка,
допуск ±1. Кнопка открывает/прячет лист. Таймер варки ×2.
Порт: `InspectorMech` через новый хук `skip_memorize(g)->bool` (пропуск фазы показа); панель
со спекой + кнопка «📋 ДОПУСКИ». УР.4-загадка-допрос и проза/перемешивание — TODO.

### gourmet_vega — Гурман с Веги (дегустация) ✅ ГОТОВО (база) / 🟡 (УР.4)
База: «ГОТОВО» → «ДЕГУСТИРОВАТЬ»; первая суб-годнота НЕ завершает раунд (одна переигровка тем
же таймером), дальше финиширует как есть; по таймеру — всегда финиш.
Порт: `GourmetMech` через новый хук `on_done(g)->bool` (false = не финишировать). УР.4-подсветка
близости + двойная проба + lock точных — TODO.

### dj_pulsar — Диджей Пульсар (эмбиент-бит) ✅ ГОТОВО
Механики-игры НЕТ. Синтезированный бит + пульс интерфейса в такт. Скоринг не трогает.
Порт: `DjMech` — кик синтезируется в `AudioStreamWAV` (синус 140→46 Гц, экспо-затухание, без
ассета), бьёт каждые 0.5с через `process`; пульс = scale-pop банки и колонок-ползунков.
TODO: гравитация ползунков (УР.4), пульс в фазе показа (process сейчас только в «ВОССОЗДАЙ»).

### vex — Векс (расстановка сгустков) 🔴 (новая ось скоринга `vexPosition`)
Сгустки-кляксы стоят в узлах сетки; на показе — целевая раскладка, на «ВОССОЗДАЙ» они
разлетаются, игрок перетаскивает каждый на своё место. Оценка = отдельный компонент
`vexPosition` (вес 0.4) по совпадению позиций.
Порт: draggable-сгустки + новая ось оценки «позиция». Требует расширения скоринга. 🔴

---

## Особые типы (меняют набор ползунков, `cfg.special`)

### nebula_chef — shape 🔴
Добавляет ползунок ФОРМА банки (`shape`) на всех уровнях. В Godot формы банки как параметра нет
(банка одной формы). Нужен параметр `shape` + профили формы. 🔴

### twofaced_priestess — gradient + dual count 🔴 (нужен `countB`)
`LEVEL4_FX`: банка делится на 2 половины, у каждой свой счётчик (макс. 7). Добавляет `countB`.
Также special 'gradient' (2-й цвет `colorB`).
Порт: нужен второй счётчик (и/или градиент). 🔴

### plasma_bartender — moving + speed 🔴 (нужен `speed`)
Эксклюзивный ползунок СКОРОСТЬ; сгустки летают внутри банки, скорость полёта читается с ползунка
в реальном времени (физика отскоков от стенок). УР.4-регулятор `speed`.
Порт: физика летающих сгустков в банке + ползунок скорости (в Godot `speed` нет — добавить). 🔴

### supernova_child — dual_size + rotation 🔴 (нужны `size2`,`rotation`)
Габарит распадается на ширину (`size`) и высоту (`size2`); УР.4 добавляет эксклюзивный ползунок
ПОВОРОТ (`rotation`, ≥30° заметно).
Порт: нужны размеры по осям и поворот банки. 🔴

### the_waiter — no_timer / песочные часы 🔴 (форма + без таймера)
`setup`: форма банки = «песочные часы» (SHAPE_PROFILES[4]). Метроном/угадай-секунды убраны.
Награда за строгие 100% — в `finalizeResult` (waiterThresholdOverride).
Порт: форма-часы (нет параметра формы) + режим без таймера + бонус за идеал. 🔴/🟡

### last_of_ir — trust 🟡 (эффекты вне механики)
`LEVEL4_FX.last_of_ir = {}` — пусто. Баффы/дебаффы (IR_EFFECTS) реализованы прямо в
startOrder/finalizeResult (доверие: часть параметров «на веру», без показа).
Порт: эффекты доверия в логике заказа, отдельной механики-виджета нет. 🟡

---

## Только УР.4 (не в MECH_FROM_L1)

### archivist — Хранитель Архива (печати) ✅ ГОТОВО
На «ВОССОЗДАЙ» по очереди «запечатывает» (блокирует) ОДИН ползунок: первый — случайный, далее —
только уже выставленный ВЕРНО (≥0.9), чередуя верные по кругу; если верных нет — печати нет.
Смена каждые 5с.
Порт: `ArchivistMech` — через новый `process(g,delta)`-хук (аккумулятор 5с), lock слайдера
(`editable=false`+🔒 в ярлыке), выбор по `g._key_score(k) >= 0.9`.
Визуал: `startMatrixRain` (падающие колонки глифов поверх банки в обеих фазах, `MATRIX_GLYPHS`,
мятный `#7dffb8` @ .48) → порт `scripts/matrix_rain.gd`, монтируется в `jar_stage` из `memorize_start`.

### intern_beep — Стажёр Бип
Механики НЕТ (обучающий). Обычные ползунки на всех уровнях.

---

## Приоритеты порта (предложение)

1. **Расширить каркас**: per-frame `process(delta)` — ГОТОВО. Осталось `score_bonus()`, `replace_memorize()`.
2. **Без нового параметра (✅), быстрые**: fashionista ✅, archivist ✅, catlady ✅, logic9-степпер ✅. Далее: engineer, marketer, gourmet, dj_pulsar.
3. **Средние (✅/🟡)**: engineer (зоны+указатель), marketer (хаос-панель), racer_kai, apothecary_mo, janitor, gourmet_vega, dj_pulsar.
4. **Требуют новой оси/фазы (🟡)**: guild_inspector, collector_gz, swarm_navigator, drone, vex.
5. **Требуют нового параметра (🔴)**: twofaced(countB), plasma(speed), supernova(size2/rotation), nebula/waiter(shape). Решить: адаптировать или добавлять параметры. (perfumer разблокирован — `sat` добавлен.)
